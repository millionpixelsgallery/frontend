import px from 'utils/style/px'
import when from 'utils/style/when'

const theme = {
  component: {},
  color: {
    NavLink: {
      activeLine: '#5E72EB',
      hoverLine: '#EAEAEA',
    },
  },
  px,
  when,
}

export default theme
